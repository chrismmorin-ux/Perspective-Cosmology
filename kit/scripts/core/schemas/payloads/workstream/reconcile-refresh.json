{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cwos.local/schemas/payloads/workstream/reconcile-refresh.json",
  "title": "workstream.reconcile-refresh",
  "description": "T6:workstream reconcile-refresh event (WS-207 reducer auto-refresh hook). WS-210.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "type": { "type": "string", "const": "reconcile-refresh" },
    "domains_intended": { "type": "array", "items": { "type": "string" } }
  }
}
