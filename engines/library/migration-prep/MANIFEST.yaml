# Engine Extension Pack Manifest
engine_id: migration-prep
name: Migration Preparation
description: Map queries, callers, and rollback paths before a database or data migration — zero impact guaranteed
category: preparation
impact: zero-impact
safety_guarantee: "This engine reads your codebase and documents what it finds. It makes NO code changes, NO database changes, and NO state changes."
archetypes: [saas, api-service, data-platform]
install_conditions: database or data migration patterns detected
programs: []
personas_required: [architect, failure-engineer, senior-engineer]
files:
  - SKILL.md

# --- Registry fields (used by /adopt to generate engine entries) ---
skill_path: .claude/commands/migration-prep.md
procedure: suite-check
extends: context-gather
trigger: manual
inputs: [codebase, system_model, migration_target]
outputs: [readiness-report]
chains:
  on_complete: null
  on_finding: null
finding_severities: null
